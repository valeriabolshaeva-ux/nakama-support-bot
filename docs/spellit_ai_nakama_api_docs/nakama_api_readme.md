# nakama_api — Сервис анализа звонков

## 📚 Как читать документацию

Документация состоит из 4 файлов. Читай в таком порядке:

| # | Файл | Описание | Когда читать |
|---|------|----------|--------------|
| 1 | **nakama_api_readme.md** | Общее описание сервиса и его возможностей | 👈 Сейчас — для понимания что это за система |
| 2 | **nakama_api_rest_integration.md** | REST API: эндпоинты, авторизация, примеры | Если нужно подключиться к API |
| 3 | **nakama_api_database_schema.md** | Структура БД, SQL запросы, парсинг данных | Если нужно работать с БД напрямую |
| 4 | **nakama_api_quick_reference.md** | Шпаргалка: основные эндпоинты и SQL | Когда уже разобрался, для быстрого доступа |

**Рекомендация:** Начни с этого файла, затем выбери нужный способ интеграции (API или БД).

---

## Что это?

**nakama_api** — backend-сервис для автоматизированного анализа качества звонков менеджеров по продажам. Система транскрибирует аудио/видео записи разговоров, анализирует их по настраиваемым критериям через AI и формирует отчёты с оценками для роста продаж.

---

## Основные возможности

### 🎙️ Транскрибация
- Поддержка аудио (mp3, wav, m4a, ogg) и видео (mp4, webm, mov) файлов
- Работа с **длинными файлами** (2+ часа) — автоматическая нарезка и склейка
- **Мультисервисная транскрибация** — одновременное использование нескольких сервисов:
  - Synopsis (основной)
  - SpeakAI
  - Nexara
- **Умное объединение транскрипций** — AI выбирает лучшие фрагменты из разных источников для идеальной транскрипции
- Определение спикеров (диаризация)
- Сохранение транскрипции в Google Docs

### 📊 Анализ по критериям
- **Неограниченное количество критериев** — настраиваются через Google Sheets
- **GPT-анализ** — каждый критерий оценивается с обоснованием и цитатами
- **Типы критериев**:
  - `score` — числовая оценка (0-5, 0-10, и т.д.) с полями: score, reason, quote
  - `insight` — текстовый инсайт без числовой оценки (только reason и quote)
- **PII-детекция** — автоматическое скрытие персональных данных (имён, телефонов, email) перед анализом
- **Кастомные формулы** — расчёт итоговых баллов по формулам с переменными:
  - `score_N` — балл N-го критерия
  - `file_duration` — длительность файла в секундах
  - CRM-поля по ключам

### 📑 Отчётность
- Автоматическая запись результатов в Google Sheets
- Шаблоны отчётов
- CRM-интеграция — привязка данных из CRM к звонкам

### ⚙️ Архитектура
- **FastAPI** — REST API
- **PostgreSQL** — основное хранилище данных
- **Celery + Redis** — асинхронная обработка файлов
- **S3** — хранение файлов
- **Google Drive/Sheets** — интеграция для отчётов

---

## Как работает pipeline обработки

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           PIPELINE ОБРАБОТКИ                                │
└─────────────────────────────────────────────────────────────────────────────┘

1. ЗАГРУЗКА ФАЙЛА
   └── POST /api/item-set + файлы
       └── Создаётся ItemSet (звонок) → Items (файлы) → загрузка в S3
           └── status: "added"

2. ТРАНСКРИБАЦИЯ (Processor: transcribe)
   └── Для каждого Item:
       ├── Нарезка длинных файлов (если > N минут)
       ├── Отправка в транскрибаторы (Synopsis, SpeakAI, Nexara)
       └── Сохранение в item_processing_output
   └── status: "processing" → "processed"

3. ОБЪЕДИНЕНИЕ ТРАНСКРИПЦИЙ (Processor: transcript_merger)
   └── Для ItemSet:
       ├── Сбор транскрипций всех Items
       ├── AI-объединение лучших фрагментов
       ├── Определение спикеров
       └── Сохранение в item_set_processing_output + Google Doc
   └── status_within_project: "all_items_processed"

4. АНАЛИЗ ПО КРИТЕРИЯМ (Processor: criteria_gpt)
   └── Для ItemSet в контексте Project:
       ├── Загрузка критериев из Google Sheets
       ├── Отправка транскрипции + критерии в GPT
       ├── Парсинг ответа (score, reason, quote для каждого критерия)
       ├── Расчёт формул
       └── Сохранение в project_item_set_processing_output + formula_output
   └── status_within_project: "processing" → "processed"

5. ФОРМИРОВАНИЕ ОТЧЁТА
   └── Запись результатов в Google Sheets
   └── Сохранение ссылок в project_processing_output
```

---

## Структура данных

### Иерархия сущностей

```
User (Пользователь)
  │
  └── Project (Проект с настройками критериев)
        │
        ├── ProjectFormula (Формулы расчёта)
        │
        └── ItemSet (Звонок = набор файлов)
              │
              ├── Item (Отдельный файл)
              │     └── ItemProcessingOutput (Транскрипция файла)
              │
              ├── ItemSetProcessingOutput (Объединённая транскрипция)
              │
              └── ProjectItemSetProcessingOutput (Результаты анализа критериев)
                    │
                    └── FormulaOutput (Результаты формул)
```

### Ключевые понятия

| Сущность | Описание | Пример |
|----------|----------|--------|
| **User** | Пользователь системы | Компания "Ригинтел" |
| **Project** | Проект с набором критериев | "Анализ B2B продаж" |
| **ItemSet** | Один звонок (может содержать несколько файлов) | "Звонок_2025-01-08_abc123" |
| **Item** | Отдельный аудио/видео файл | "record_part1.mp3" |
| **Criterion** | Критерий оценки | "Приветствие и установление контакта" |
| **Formula** | Формула расчёта итогового балла | "(score_1 + score_2) / 2 * 20" |

---

## Два способа работы с данными

### 1. REST API (рекомендуется)

Для интеграций, получения данных, отправки файлов на обработку.

**Авторизация:**
```http
API-Access-Key: <your_api_key>
```

**Основные эндпоинты:**
```
GET /api/insights                    — Результаты анализа критериев
GET /api/transcription              — Транскрипция звонка
GET /api/client/project/{id}/item-sets — Список звонков проекта
GET /api/projects                   — Список проектов
```

📄 **Полная документация:** `nakama_api_rest_integration.md`

### 2. Прямой доступ к БД (для аналитики)

Для сложных SQL-запросов, аналитики, построения отчётов.

**Подключение:**
```
postgresql://<user>:<password>@<host>:<port>/<database>
```

**Ключевые таблицы:**
```
"user".base_user                            — Клиенты (пользователи)
object.project                              — Проекты
object.item_set                             — Звонки
output.project_item_set_processing_output  — Результаты анализа критериев ⭐
output.item_set_processing_output          — Транскрипции
output.formula_output                       — Результаты формул
```

**Быстрый старт:**
```sql
-- Список всех клиентов
SELECT id, name, email FROM "user".base_user WHERE is_active = true;

-- Список всех проектов с клиентами
SELECT p.id, p.name, u.name as client FROM object.project p
JOIN "user".base_user u ON u.id = p.id_base_user;

-- Звонки проекта
SELECT id, name, status_within_project FROM object.item_set WHERE id_project = 1;
```

📄 **Полная документация:** `nakama_api_database_schema.md`

---

## Технические особенности

### Обработка длинных файлов
- Файлы > 25 МБ автоматически нарезаются на части
- Каждая часть обрабатывается отдельно
- Результаты склеиваются с сохранением таймкодов

### Мультисервисная транскрибация
- Один файл может обрабатываться несколькими сервисами параллельно:
  - **Synopsis** — основной сервис транскрибации
  - **Nexara** — дополнительный сервис (лучше распознаёт имена)
  - **SpeakAI** — альтернативный сервис
  - **Zoom Transcript** — импорт готовой транскрипции из Zoom
- AI (transcript_merger) анализирует результаты и выбирает лучшие фрагменты
- Итоговая транскрипция — комбинация лучших частей из всех источников

### Хранение транскрипций
- **Сырые** транскрипции от каждого сервиса хранятся отдельно в `output.item_processing_output`
- **Очищенные** транскрипции (нормализованные таймстампы, спикеры) вычисляются на лету
- **Объединённая** транскрипция хранится в `output.item_set_processing_output`

> 📄 **Подробнее о форматах и парсинге:** `nakama_api_database_schema.md` → раздел "Сырые транскрипции"

### Асинхронная обработка
- Все тяжёлые задачи выполняются через Celery
- Клиент получает ID задачи и может проверять статус
- Retry-механизм при ошибках (до 5 попыток)

### Статусы обработки

| Поле | Что отслеживает |
|------|-----------------|
| `item.status` | Транскрибация файла |
| `item_set.status` | Транскрибация всех файлов |
| `item_set.status_within_project` | Анализ по критериям |
| `project.active_status` | Состояние проекта |

**Финальный статус для получения результатов:**
```python
if item_set.status_within_project == "processed":
    # Данные готовы!
```

### Автоматический экспорт в Google

После завершения обработки nakama автоматически записывает результаты:

| Данные | Куда экспортируются |
|--------|---------------------|
| Транскрипция | Google Docs (отдельный документ на каждый звонок) |
| CRM данные | Google Sheets, столбцы с пользовательскими названиями |
| Оценки критериев | Google Sheets, лист "AI" |
| Обоснования (reason) | Google Sheets, рядом с оценками |
| Цитаты (quote) | Google Sheets, рядом с обоснованиями |
| Результаты формул | Google Sheets, отдельные колонки |

**URL отчёта:**
```sql
SELECT output_data->>'report_url' FROM output.project_processing_output WHERE id_project = 1;
```

**URL транскрипции:**
```sql
SELECT output->>'google_doc' FROM output.item_set_processing_output WHERE id_item_set = 50;
```

### Баланс и лимиты
- У каждого пользователя есть баланс минут
- При исчерпании баланса обработка останавливается
- Режим `basic` — уходить в минус, `strict` — останавливать

---

## Файлы документации

| Файл | Описание | Когда использовать |
|------|----------|-------------------|
| **nakama_api_readme.md** | Этот файл — общее описание системы | Первое знакомство с сервисом |
| **nakama_api_rest_integration.md** | Полное описание REST API | Интеграция через API |
| **nakama_api_database_schema.md** | Структура БД, таблицы, SQL | Прямые SQL-запросы, аналитика |
| **nakama_api_quick_reference.md** | Шпаргалка | Быстрый поиск эндпоинта/запроса |

---

## Быстрый старт

### Вариант 1: Получить данные через API

```python
import requests

# Настройка
BASE_URL = "https://<domain>/api"
HEADERS = {"API-Access-Key": "<your_api_key>"}
PROJECT_ID = 1

# Получить список проанализированных звонков
response = requests.get(
    f"{BASE_URL}/client/project/{PROJECT_ID}/item-sets",
    headers=HEADERS
)
item_sets = response.json()["item_sets"]

# Для каждого готового звонка получить результаты
for item_set in item_sets:
    if item_set["status_within_project"] == "processed":
        # Результаты анализа критериев
        insights = requests.get(
            f"{BASE_URL}/insights",
            params={"id_project": PROJECT_ID, "id_item_set": item_set["id"]},
            headers=HEADERS
        ).json()
        
        print(f"\n=== {item_set['name']} ===")
        for insight in insights["insights"]:
            print(f"  {insight['criterion_name']}: {insight['score']}")
```

### Вариант 2: SQL-запрос к БД

```sql
-- Получить все проанализированные звонки с оценками
SELECT 
    iss.name as call_name,
    iss.created_at,
    piso.output as criteria_results
FROM object.item_set iss
JOIN output.project_item_set_processing_output piso 
    ON piso.id_item_set = iss.id
WHERE iss.id_project = 1
  AND iss.status_within_project = 'processed'
ORDER BY iss.created_at DESC;
```

---

## Что можно делать с данными

### 📈 Аналитика продаж
- Средний балл по критериям за период
- Динамика качества звонков по менеджерам
- Выявление слабых мест в скриптах продаж

### 🔄 Интеграции
- Синхронизация с CRM (AmoCRM, Bitrix24)
- Автоматическая загрузка записей из телефонии
- Webhook-уведомления о завершении анализа

### 📊 Отчёты
- Еженедельные отчёты по качеству
- Сравнение менеджеров
- Тренды по критериям

---

## 🚀 Следующие шаги

Теперь, когда ты понял что такое nakama_api, выбери свой путь:

### Хочу подключиться через API
→ Читай **`nakama_api_rest_integration.md`**
- Авторизация (API-Key / JWT)
- Эндпоинты для получения данных
- Примеры на Python, JavaScript, cURL

### Хочу работать напрямую с БД
→ Читай **`nakama_api_database_schema.md`**
- Схема таблиц
- SQL запросы для получения данных
- Парсинг сырых транскрипций и критериев

### Уже разобрался, нужна шпаргалка
→ Открой **`nakama_api_quick_reference.md`**
- Основные эндпоинты в одном месте
- Частые SQL запросы

---

## Контакты

Для получения:
- API-ключа
- Доступа к БД
- ID проекта

Обратитесь к администратору системы.
