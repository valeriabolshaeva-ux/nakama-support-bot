# Техническое задание: Telegram Support Bot

## 1. Цель проекта

Telegram-бот для поддержки клиентов, который:
- Принимает обращения клиентов
- Автоматически привязывает пользователя к клиенту/проекту через invite-code
- Создаёт отдельный тред (topic) в группе поддержки для каждого обращения
- Пересылает переписку и вложения между клиентами и операторами
- Даёт операторам кнопки управления статусами
- Отправляет ответы операторов клиенту от имени бота

---

## 2. Роли

| Роль | Описание |
|------|----------|
| **Client** | Пользователь, пишущий боту |
| **Operator** | Оператор поддержки (2 человека), работает в группе поддержки |
| **Admin** | Тот же operator, управляет справочником invite-codes |

---

## 3. Основные сущности данных

| Сущность | Описание |
|----------|----------|
| **ClientCompany** | Компания-клиент |
| **Project** | Проект/контур внутри компании |
| **InviteCode** | Код привязки к проекту |
| **Ticket** | Обращение |
| **Message** | Сообщения в рамках тикета |
| **Feedback** | Оценка после закрытия |

---

## 4. Каналы

- **Вход:** Личный чат клиент ↔ бот
- **Внутренний канал:** Telegram Group "Support Team" с включёнными topics
- **Каждое обращение = отдельный topic (thread) в Support Team**

---

## 5. Идентификация клиента / проекта (invite-code)

### 5.1 Механика
- Клиент заходит по ссылке `t.me/<bot>?start=<INVITE_CODE>` или пишет `/start <INVITE_CODE>`
- Бот валидирует code в справочнике `projects.invite_code`
- Если валиден: сохраняет привязку `user_id → project_id` (+ `client_id`)
- Если нет code или code неверный: запускает triage

### 5.2 Несколько проектов одного клиента
- У одного `client_id` может быть несколько `project_id`
- Для каждого проекта свой `invite_code`
- Команда `/project` показывает список доступных проектов и даёт переключить активный

### 5.3 Unknown / triage
Если пользователь пришёл без invite-code или с неверным:
- Бот: "Привет! Чтобы я направил запрос правильно, нужен код проекта. Если кода нет — нажмите 'Нет кода'."
- Кнопки: **[Ввести код]** **[Нет кода]**
- "Ввести код" → ожидание текста кода
- "Нет кода" → бот просит: Компания/проект (текст), Контакт (email/телефон, опционально)
- Создаётся тикет типа ACCESS/TRIAGE в отдельном topic "UNKNOWN/TRIAGE"

---

## 6. Категории запросов (меню)

Inline keyboard кнопки:
- A) Не работает / ошибка (баг)
- B) Данные не сходятся / аналитика
- C) Интеграции и источники данных
- D) Доступы и роли
- E) Настройка и использование (как сделать…)
- F) Оплата и документы
- G) Запрос на улучшение
- H) Срочно (критично)
- I) Другое

---

## 7. SLA-тексты и правила ожиданий

### 7.1 В интро и подтверждении обращения:
- **Рабочие часы:** Пн–Пт 10:00–19:00 (Europe/Madrid)
- **Первый ответ:** обычно 2–4 часа в рабочее время
- **Срочно:** стараемся 30–60 минут в рабочее время
- **Вне рабочего времени:** следующий рабочий день

### 7.2 Тональность:
- Короткие фразы
- Лёгкий вайб, но без шуток в стиле стендапа
- Если "Срочно" — юмор не использовать

---

## 8. User Flow клиента

### 8.1 /start (с кодом)
- Приветствие
- Коротко: что здесь происходит
- Показать меню категорий

**Пример интро:**
> "Привет! Я бот поддержки. Я аккуратно соберу детали и передам задачу команде — ничего не потеряется. Выберите, что случилось."

### 8.2 Создание тикета
1. **Шаг 1:** Выбор категории
2. **Шаг 2:** Описание проблемы (free text)
3. **Шаг 3:** "Можно прикрепить скрин/видео/файл" (бот ждёт файлы или кнопку "Пропустить")
4. **Шаг 4:** Подтверждение "Готово, обращение #NNNN принято" + краткий SLA

### 8.3 Уточнения для "Срочно"
Если категория = Срочно:
- **Вопрос 1:** "Насколько блокирует работу?"
  - A) Полностью блокирует
  - B) Частично мешает
  - C) Не блокирует, но важно
- **Вопрос 2:** "Что именно не работает?" (текст)
- Затем шаг с вложениями

### 8.4 Активное обращение
- В v1: у пользователя максимум 1 активный открытый тикет
- Если тикет открыт и клиент пишет — всё добавляется в этот тикет
- Если тикет закрыт и клиент пишет — бот спрашивает:
  - A) Открыть новый тикет
  - B) Открыть старый тикет заново (если прошло < 48 часов)

---

## 9. Support Group (куда приходят запросы)

### 9.1 При создании тикета бот:
- Создаёт topic в Support Group
- Название topic: `#NNNN | <ClientName> | <ProjectName> | <Category>`
- Первое сообщение: карточка тикета

### 9.2 Формат карточки тикета

```
● Ticket: #NNNN
● Клиент/проект: <client> / <project>
● Пользователь: @username (id: xxxx)
● Категория: <category>
● Приоритет: normal или urgent
● Время: YYYY-MM-DD HH:MM (Europe/Madrid)
● Описание: текст клиента
● Вложения: да/нет (и типы)
● Статус: New

Кнопки: [Взять в работу] [Закрыть] [Запросить детали]
```

---

## 10. Ответы операторов → клиенту

### 10.1 Правило
- Операторы отвечают в topic
- Бот пересылает текст/вложения операторов клиенту
- Сообщения идут "как ответ поддержки" без упоминаний внутренней кухни

### 10.2 Ограничение доступа
- Только `user_id` из списка `OPERATORS` может триггерить отправку клиенту
- Если не-оператор пишет в topic — бот игнорирует

### 10.3 "Запросить детали" (кнопка)
Бот отправляет клиенту шаблон:
> "Нужно чуть больше деталей, чтобы быстрее помочь:
> 1. что вы делали перед проблемой
> 2. ссылка/экран/раздел
> 3. скрин/видео (если можно)"

---

## 11. Статусы тикета

| Статус | Описание |
|--------|----------|
| `new` | Новый тикет |
| `in_progress` | В работе |
| `closed` | Закрыт |

### Логика кнопок:
- **Взять в работу** → `in_progress`, `assigned_to = operator_id`
- **Закрыть** → `closed`, `closed_at = now`, отправить клиенту: "Кажется, решили. Если что-то останется — просто напишите сюда." + CSAT
- Если закрыто и пришло новое сообщение клиента → предложить reopen/new

---

## 12. CSAT после закрытия

- После закрытия бот спрашивает: "Оцените, пожалуйста, как мы помогли"
- Кнопки: **👍** / **👎**
- Если 👎 → спросить "Что было не так?" (текст)
- Сохранить feedback в БД
- Отправить feedback в topic тикета отдельным системным сообщением

---

## 13. Вложения

- Принимать фото/видео/файлы/голосовые
- Проксировать в topic тикета
- Проксировать от операторов клиенту
- В БД хранить метаданные: `file_id`, `type`, `size`, `timestamp`

---

## 14. Хранилище (SQLite v1)

### 14.1 Таблицы

**clients**
- id, name

**projects**
- id, client_id, name, invite_code, is_active

**user_bindings**
- id, tg_user_id, tg_username, tg_name, project_id, created_at, updated_at

**tickets**
- id, number, project_id, tg_user_id, category, priority, status
- support_chat_id, topic_id
- assigned_to_tg_user_id (nullable)
- created_at, updated_at, first_response_at (nullable), closed_at (nullable)

**messages**
- id, ticket_id, direction, tg_message_id, type, text, file_id, author_tg_user_id, created_at

**feedback**
- id, ticket_id, csat, comment, created_at

### 14.2 Минимальные операции
- create ticket
- append message
- update status
- bind user to project
- list user active ticket
- save feedback

---

## 15. Конфиг и ENV

### ENV переменные:
```
BOT_TOKEN=
SUPPORT_CHAT_ID=
OPERATORS=user_id1,user_id2
TIMEZONE=Europe/Madrid
DB_PATH=./data.sqlite
LOG_LEVEL=info
```

### Конфиг-файл (json/yaml):
- categories
- тексты интро/шаблонов
- рабочие часы и SLA-тексты

---

## 16. Edge cases (обработать явно)

- пользователь без username
- пользователь пришёл без кода
- неверный код
- клиент пишет после закрытия тикета
- оператор нажал "взять в работу", а потом другой тоже нажал
- бот потерял topic_id (ошибка Telegram API)
- вложение без текста
- несколько вложений подряд
- спам/флуд от unknown

---

## 17. Acceptance Criteria

- [ ] Любой тикет создаёт отдельный topic в группе
- [ ] Любое сообщение клиента попадает в правильный topic
- [ ] Ответ оператора из topic приходит клиенту
- [ ] Статусы меняются кнопками
- [ ] CSAT собирается после закрытия
- [ ] Unknown идут в triage topic
- [ ] В БД видна история тикетов и статусы

---

## 18. Подготовка до разработки

- [ ] Создать бота в BotFather, получить token
- [ ] Создать Telegram-группу Support Team, включить topics
- [ ] Добавить бота в группу и дать админ-права
- [ ] Узнать user_id двух операторов
- [ ] Составить первый список projects + invite_code (3–10 штук)

---

*Документ сохранён: 2026-02-05*
