# ==============================================================================
# 📘 CURSOR RULES — Универсальный проект
# ==============================================================================
# Правила для AI-агентов в Cursor IDE
# Версия: 1.0 | Актуально для: 2026
# ==============================================================================

## 🎯 Общие правила

### Перед началом работы ОБЯЗАТЕЛЬНО:
1. Прочитай `docs/project-plan.md` — пойми общий план
2. Прочитай `docs/technical-summary.md` — пойми архитектуру
3. Прочитай `docs/architecture.md` — пойми структуру системы
4. Осознай своё место и задачу в контексте всего проекта
5. **Только после этого приступай к выполнению!**

### Язык
- Код, комментарии, docstrings — на **английском**
- Документация и changelog — на **русском**
- Общение с пользователем — на **русском**

## 🔐 Секреты и конфигурация

### Золотые правила:
- ✅ Секреты — ТОЛЬКО в .env (никогда не коммитим!)
- ✅ В репозитории — ТОЛЬКО .env.example (с плейсхолдерами)
- ✅ Валидация конфига — через Pydantic Settings
- ❌ НИКОГДА не хардкодить API ключи, пароли, токены в код
- ❌ AI-агенты НИКОГДА не создают/редактируют .env (только .env.example)

## 🐍 Backend (Python/FastAPI)

### Версия и типизация:
- Python 3.11+
- Type hints: ОБЯЗАТЕЛЬНЫ для всех функций и методов

### Именование:
- `PascalCase` — классы
- `snake_case` — функции, методы, переменные
- `UPPERCASE` — константы

### Порядок импортов:
1. Standard library
2. Third-party
3. Local application

### Docstrings (ОБЯЗАТЕЛЬНО!):
- Каждая функция/метод ДОЛЖНА иметь docstring
- Формат: описание, Args, Returns, Raises, Example

### Комментарии в коде:
- Используй разделители: `# === НАЗВАНИЕ БЛОКА ===`
- Объясняй "почему", а не "что"

### Зависимости:
- FastAPI, Pydantic, SQLAlchemy, asyncpg
- Celery + Redis для фоновых задач
- pytest для тестов

## ⚛️ Frontend (React/TypeScript)

### Структура: Feature-Sliced Design + shadcn/ui
- `src/app/` — инициализация, провайдеры, роутер
- `src/components/ui/` — shadcn компоненты
- `src/entities/` — бизнес-сущности
- `src/features/` — пользовательские сценарии
- `src/lib/` — утилиты (cn функция)
- `src/pages/` — страницы
- `src/shared/` — общие компоненты и хуки
- `src/widgets/` — виджеты

### Стилизация:
- ✅ Tailwind CSS utility классы
- ✅ cn() функция для условных классов
- ✅ CSS переменные для темы
- ❌ НЕ используем CSS Modules
- ❌ НЕ используем inline styles

### Компоненты:
- shadcn/ui — базовые UI компоненты
- Lucide React — иконки
- react-hook-form + zod — формы
- TanStack Table — таблицы

## 🧪 Тестирование

### Минимальные требования:
- Каждый endpoint = минимум 5 тестов
- Каждый сервис = минимум 3 теста

### Маркеры pytest:
- `@pytest.mark.unit` — быстрые изолированные
- `@pytest.mark.integration` — требуют БД/Redis
- `@pytest.mark.slow` — долгие (> 5 сек)

### Перед коммитом:
```bash
pytest -v --tb=short
```

## 🔀 Git

### Формат коммитов (Conventional Commits):
- `feat` — новая функциональность
- `fix` — исправление бага
- `docs` — документация
- `style` — форматирование
- `refactor` — рефакторинг
- `test` — тесты
- `chore` — обслуживание

### Примеры:
```
feat(auth): add JWT refresh token support
fix(api): correct validation for email field
docs(readme): update installation instructions
```

## 📁 Структура проекта

```
project_root/
├── .cursorrules              # Правила для Cursor AI
├── .cursor/rules/            # Детальные правила для агентов
├── .env.example              # Пример переменных окружения
├── .gitignore
├── .flake8
├── README.md
├── docker-compose.yml
├── docker-compose-dev.yml
├── Dockerfile
│
├── backend/                  # Python/FastAPI бэкенд
│   ├── app/
│   ├── tests/
│   ├── requirements.txt
│   └── settings.py
│
├── frontend/                 # React/TypeScript фронтенд
│   ├── src/
│   ├── package.json
│   └── vite.config.ts
│
├── docs/                     # Документация
│   ├── project-plan.md
│   ├── technical-summary.md
│   ├── architecture.md
│   ├── agent-prompts.md
│   └── changelogs/
│
├── scripts/                  # Вспомогательные скрипты
└── temporary/                # Временные файлы (не коммитим)
```

## 📝 Changelog агента

После выполнения задачи создай файл `docs/changelogs/wave-X-agent-Y.md`:
- Что сделал
- Какие файлы создал/изменил
- Как проверить работу
- Известные ограничения

## 🚫 Запрещено

- Хардкодить секреты
- Редактировать .env файлы
- Создавать файлы вне своей зоны ответственности
- Пропускать тесты
- Коммитить без осмысленного сообщения
